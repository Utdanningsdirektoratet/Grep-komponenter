/*
    Without these overrides, storybook won`t run.
    For react-scripts to work jsx needs to be "preserve",
    isolatedModules needs to be: "true" and noEmit needs to be "true".
    Ref: https://github.com/storybooks/storybook/issues/4739
*/

{
  "extends": "../tsconfig.test.json",
  "compilerOptions": {
    "jsx": "react",
    "isolatedModules": false,
    "noEmit": false
  }
}
