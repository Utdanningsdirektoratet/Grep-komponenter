import React from 'react';
import Standard from './standard';

export default {
  title: 'NavGuard',
  render: () => <Standard />
};

export const Default = () => {
  return <Standard />;
};

Default.storyName = 'Default';
