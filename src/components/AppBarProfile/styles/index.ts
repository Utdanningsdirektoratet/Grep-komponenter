import { makeStyles } from '../../../styling';

export const useStyles = makeStyles()({
  user: {
    margin: '0 10px',
    marginLeft: 20,
    height: 'fit-content',
    textAlign: 'left',
    backgroundColor: 'inherit',
    textTransform: 'none',
  },
});
