import CleanPaper from '../CleanPaper';
import withStyles from '@material-ui/core/styles/withStyles';

export const StyledAppBarTop = withStyles({
  root: {
    width: '100%',
    display: 'flex',
    justifyContent: 'space-between',
  },
})(CleanPaper);
